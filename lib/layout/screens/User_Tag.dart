import 'package:flutter/material.dart';

class UserTag extends StatelessWidget {
  const UserTag({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('User Tag'));
  }
}
